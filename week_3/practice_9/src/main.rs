fn main() {
    let mut fees:i32 = 25_000;      //mut is used for mutable variables(they can change)
    println!("fees is {}",fees);

    fees = 35_000;
    println!("fees changed to {}",fees);    
}
